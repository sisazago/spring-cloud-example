spring :
    application :
        name : config-client
    profiles :
        active : development
    cloud:
        config:
            uri : http://localhost:8888
            username : root
            password : root